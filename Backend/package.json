{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/server.js",
    "dev": "nodemon --watch src/server.js",
    "db:schema": "psql -U postgres -d folkmint -f src/schema/FolkMint.schema.sql",
    "db:seed": "node src/schema/runSeed.js",
    "db:reset": "npm run db:schema && npm run db:seed",
    "db:test": "node src/test.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "\\",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "nodemon": "^3.1.11",
    "pg": "^8.17.2",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "playwright": "^1.53.0"
  }
}
